services:
  - type: web
    name: video-streamer-backend
    env: node
    buildCommand: cd ../.. && npm run build --workspace=packages/shared && cd apps/backend && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000